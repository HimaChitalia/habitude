<%= form_for(@category) do |f| %>
<%= render 'layouts/errors', form: @category %>


  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to 'Back', categories_path %>
