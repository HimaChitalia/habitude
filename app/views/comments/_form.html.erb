<%= form_for [comment.habit, comment] do |f| %>
  <%= render 'layouts/errors', form: comment %>
  Description: <%= f.text_area :description, size: "10x5"  %> <br>
  <%= f.hidden_field :user_name, :value => current_user.name %>
  <%= f.submit data: { disable_with: false } %>
<% end %>
