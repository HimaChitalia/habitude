<% if current_user == @habit.user %>
  <% habit_id = @habit.id %>
  <% goal_id = goal.id %>
  <a href="/habits/<%= habit_id %>/goals/<%= goal_id %>" data-confirm="Destroy gaol: '<%= goal.name %>'?" data-method="delete" class="jquery-postback" rel="nofollow">Delete Goal</a>
<% end %>
